Example endpoint
---
parameters:
  - name: name
    in: path
    type: string
    required: true
    default: all
definitions:
  Resp:
    type: object
    properties:
      my_name:
        type: string
responses:
  200:
    description: A list of colors (may be filtered by palette)
    schema:
      $ref: '#/definitions/Resp'