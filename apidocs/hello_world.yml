Example endpoint
---
parameters:
  - name: name
    in: path
    type: string
    required: true
    default: all
definitions:
  Resp:
    type: object
    properties:
      my_name:
        type: string
responses:
  200:
    description: blabla
    schema:
      $ref: '#/definitions/Resp'